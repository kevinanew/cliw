# 开发规范

- 以可读性为先。让没有开发经验的读者也能从名称、步骤和必要的注释中看懂代码及 CI 配置的主要行为。
- 使用直观的名称、明确的条件和清晰的执行顺序。不要为了减少代码行数而引入难懂的缩写、正则表达式或隐藏在配置中的规则。
- 复杂逻辑用简短注释说明目的和原因；所有代码注释使用中文。
- 文档、测试说明、CI 步骤名、错误提示、PR 标题和说明等面向人的文字，能用中文就用中文。语言语法、第三方 API、文件路径和必须保留原文的标识符除外。
